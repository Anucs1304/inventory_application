extends layout

block content
  h2 All Items
  a.button(href="/items/create") New Item
  ul
    each item in item_list
      li
        a(href=`/items/${item.id}`)= `${item.name} (${item.category_name})`
